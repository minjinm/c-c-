/*
CH-230-A
a1 p2.[c or cpp or h]
Minjinsor Munkh-Erdene
mmunkherde@jacobs-university.de
*/

#include <stdio.h>

int main () {

    int result;
    result = (2 + 7) * 9 / 3; /* The result of our calculation */
    printf("The result is %d\n", result);
    return 0;

/* the integer placeholder must be followed by its variable, 
so the 'result' should be added */

}